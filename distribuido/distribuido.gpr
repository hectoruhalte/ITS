with "xmlada";
with "polyorb";

project Distribuido is
   for Source_Dirs use ("src", "../genericos", "../estatico", "../dinamico");
   for Object_Dir use "obj";
   for Main use ("m_vehiculo.adb");
   for Exec_Dir use "bin";
end Distribuido;
